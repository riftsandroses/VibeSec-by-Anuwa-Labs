import { writable } from 'svelte/store';

export const authStore = writable(null);
export const resultsStore = writable(null);
export const profileStore = writable(null);
export const dashboardStore = writable(null);